package br.com.alura.screenmatch.calculos;

public interface Classificavel {

    //Atributo exigido em forma de métodos nas classes que vão utilizar o implements desta interface
    int getClassificacao();
}
